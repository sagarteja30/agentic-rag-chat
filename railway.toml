[railway]
name = "agentic-rag"
description = "Production RAG with AI agents"

[build]
builder = "nixpacks"
buildCommand = "pip install -r requirements.txt"

[deploy]
startCommand = "python agentic_rag.py"
minReplicas = 1
maxReplicas = 1

[variables]
PYTHONUNBUFFERED = "1"